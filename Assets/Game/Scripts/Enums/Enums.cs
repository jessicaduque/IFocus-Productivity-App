public enum TIMER_STATE // Enum para indicar o estado atual do timer no aplicativo
{
    TIMER_ON, // Timer está ativado e rodando
    TIMER_PAUSED, // Timer está ativado porém pausado
    TIMER_OFF, // Timer não está ativado no momento 
}